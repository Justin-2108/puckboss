import { initializeApp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-app.js";
import { getAuth, onAuthStateChanged, signInWithEmailAndPassword, createUserWithEmailAndPassword, updateProfile, signOut } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-auth.js";
import { getFirestore, doc, setDoc, getDoc, collection, getDocs, query, where, serverTimestamp } from "https://www.gstatic.com/firebasejs/12.1.0/firebase-firestore.js";
import { firebaseConfig } from "./firebase-config.js";

const app = initializeApp(firebaseConfig);
const auth = getAuth(app);
const db = getFirestore(app);
const MAIN_LEAGUE_ID = "puckboss-2026-27";
const MAIN_LEAGUE_NAME = "PuckBoss 2026/27";
let games = [], user = null, league = null, register = false, tipRefreshTimer = null, draftTips = {};
const $ = id => document.getElementById(id);
function esc(v){return String(v==null?"":v).replace(/[&<>\"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#039;"}[c]));}
function toast(message){const e=$("toast");if(!e)return;e.textContent=message;e.style.display="block";setTimeout(()=>e.style.display="none",2500);}
function gameDate(g){if(g.dateTime)return new Date(g.dateTime);if(g.date&&String(g.date).includes("T"))return new Date(g.date);if(g.date)return new Date(g.date+"T"+(g.time||"00:00"));return new Date("");}
function gameTime(g){if(g.time)return g.time;const value=g.dateTime||g.date||"";const match=String(value).match(/T(\d{2}:\d{2})/);return match?match[1]:"";}
function locked(g){return gameDate(g).getTime()<=Date.now();}
function scheduleTipRefresh(){if(tipRefreshTimer)clearTimeout(tipRefreshTimer);tipRefreshTimer=null;if(!league)return;const upcoming=games.map(gameDate).filter(d=>!Number.isNaN(d.getTime())&&d.getTime()>Date.now()).sort((a,b)=>a-b)[0];if(!upcoming)return;tipRefreshTimer=setTimeout(async()=>{tipRefreshTimer=null;if(league&&document.activeElement?.matches("#tipsView input")){scheduleTipRefresh();return;}if(league)await renderTips();},Math.max(250,upcoming.getTime()-Date.now()+250));}
async function loadGames(){try{const response=await fetch("data/games.json?v="+Date.now(),{cache:"no-store"});games=await response.json();}catch(error){games=[];console.error(error);toast("Spielplan konnte nicht geladen werden.");}}
async function ensureMainLeague(){const leagueRef=doc(db,"leagues",MAIN_LEAGUE_ID);const leagueSnapshot=await getDoc(leagueRef);if(!leagueSnapshot.exists())await setDoc(leagueRef,{name:MAIN_LEAGUE_NAME,ownerUid:user.uid,createdAt:serverTimestamp()});const memberRef=doc(db,"leagueMembers",user.uid+"_"+MAIN_LEAGUE_ID);await setDoc(memberRef,{uid:user.uid,leagueId:MAIN_LEAGUE_ID,displayName:user.displayName||user.email.split("@")[0],joinedAt:serverTimestamp()},{merge:true});league={id:MAIN_LEAGUE_ID,name:MAIN_LEAGUE_NAME};}
async function getTips(uid=user.uid){if(!league)return{};const result={};await Promise.all(games.map(async game=>{const snapshot=await getDoc(doc(db,"leagueTips",league.id,"users",uid,"games",game.id));if(snapshot.exists())result[game.id]=snapshot.data();}));if(uid===user.uid&&!Object.keys(result).length){const old=await getDoc(doc(db,"leagueTips",league.id,"users",uid));if(old.exists())return old.data().tips||{};}return result;}
async function saveTips(tips){if(!league||!user)return;const writes=[];for(const game of games){if(locked(game))continue;const tip=tips[game.id];if(!tip||tip.home==null||tip.away==null)continue;const home=Number(tip.home),away=Number(tip.away);if(!Number.isInteger(home)||!Number.isInteger(away)||home<0||home>10||away<0||away>10)continue;writes.push(setDoc(doc(db,"leagueTips",league.id,"users",user.uid,"games",game.id),{uid:user.uid,home,away,updatedAt:serverTimestamp()}));}await Promise.all(writes);}
function points(tip,game){if(!tip||tip.home==null||tip.away==null||game.homeScore==null)return 0;if(tip.home===game.homeScore&&tip.away===game.awayScore)return 3;if(tip.home-tip.away===game.homeScore-game.awayScore)return 2;return Math.sign(tip.home-tip.away)===Math.sign(game.homeScore-game.awayScore)?1:0;}
function gamesByDay(){const map=new Map();games.forEach(game=>{const day=String(game.date||game.dateTime).slice(0,10);if(!map.has(day))map.set(day,[]);map.get(day).push(game);});map.forEach(list=>list.sort((a,b)=>gameDate(a)-gameDate(b)));return map;}
function logo(teamName){const files={"Eisbären Berlin":"assets/teams/berlin.svg","Straubing Tigers":"assets/teams/straubing.svg","Kölner Haie":"assets/teams/koeln.svg","Grizzlys Wolfsburg":"assets/teams/wolfsburg.svg","Nürnberg Ice Tigers":"assets/teams/nuernberg.svg","Augsburger Panther":"assets/teams/augsburg.svg","Krefeld Pinguine":"assets/teams/krefeld.png","Pinguins Bremerhaven":"assets/teams/bremerhaven.svg","Iserlohn Roosters":"assets/teams/iserlohn.svg","ERC Ingolstadt":"assets/teams/ingolstadt.png","EHC Red Bull München":"assets/teams/muenchen.svg","Adler Mannheim":"assets/teams/mannheim.svg","Schwenninger Wild Wings":"assets/teams/schwenningen.svg","Löwen Frankfurt":"assets/teams/frankfurt.svg"};return files[teamName]?'<img class="team-logo" src="'+files[teamName]+'" alt="" width="42" height="42">':"";}
function teamMarkup(name,logoOnRight){const image=logo(name),text='<span class="team-name">'+esc(name)+'</span>';return logoOnRight?text+image:image+text;}
function currentTipFor(game,loadedTips){return draftTips[game.id]||loadedTips[game.id]||{};}
async function renderTips(){if(!league){$("tipsView").innerHTML='<div class="card">Tippspiel wird geladen …</div>';return;}const loadedTips=await getTips();draftTips={...loadedTips,...draftTips};let html="";gamesByDay().forEach((list,day)=>{html+='<section class="gameGroup"><h2>'+new Date(day+"T12:00:00").toLocaleDateString("de-DE",{weekday:"long",day:"2-digit",month:"2-digit",year:"numeric"})+'</h2><div class="gameList">';list.forEach(game=>{const tip=currentTipFor(game,loadedTips),homeValue=tip.home==null?"":tip.home,awayValue=tip.away==null?"":tip.away;html+='<div class="game"><div class="date"><b>'+esc(gameTime(game))+'</b><small>Spiel '+esc(game.round)+'</small></div><div class="teams"><span class="team home-team">'+teamMarkup(game.home,true)+'</span><span class="vs">vs.</span><span class="team away-team">'+teamMarkup(game.away,false)+'</span></div><div class="score">';if(locked(game))html+= '<div class="locked">'+(game.homeScore!=null?'Endstand <b>'+game.homeScore+':'+game.awayScore+'</b>':'Tipp geschlossen')+'</div>';else html+='<input class="tip-input" type="text" inputmode="numeric" pattern="[0-9]*" maxlength="2" autocomplete="off" autocorrect="off" spellcheck="false" data-id="'+esc(game.id)+'" data-side="home" value="'+esc(homeValue)+'"><b>:</b><input class="tip-input" type="text" inputmode="numeric" pattern="[0-9]*" maxlength="2" autocomplete="off" autocorrect="off" spellcheck="false" data-id="'+esc(game.id)+'" data-side="away" value="'+esc(awayValue)+'">';html+='</div></div>';});html+='</div></section>';});html+='<div class="save"><button class="primary" id="saveAll" type="button">Tipps speichern</button></div>';$("tipsView").innerHTML=html;document.querySelectorAll("#tipsView .tip-input").forEach(input=>{input.addEventListener("focus",()=>input.select());input.addEventListener("input",()=>{const clean=input.value.replace(/[^0-9]/g,"").slice(0,2),id=input.dataset.id,side=input.dataset.side;if(input.value!==clean)input.value=clean;if(!draftTips[id])draftTips[id]={};draftTips[id][side]=clean===""?null:Number(clean);});});$("saveAll").onclick=async()=>{const updated={...draftTips};let invalid=false;document.querySelectorAll("#tipsView .tip-input").forEach(input=>{const raw=input.value.trim(),id=input.dataset.id,side=input.dataset.side;if(!updated[id])updated[id]={};updated[id][side]=raw===""?null:Number(raw);if(raw!==""&&(!Number.isInteger(Number(raw))||Number(raw)<0||Number(raw)>10))invalid=true;});if(invalid){toast("Bitte nur ganze Tore von 0 bis 10 eingeben.");return;}try{await saveTips(updated);draftTips={...updated};toast("Tipps gespeichert.");await updateStats();}catch(error){console.error(error);toast(error.code==="permission-denied"?"Ein Tipp ist bereits geschlossen.":"Tipps konnten nicht gespeichert werden.");}};scheduleTipRefresh();}
async function ranking(){if(!league)return[];const snapshot=await getDocs(query(collection(db,"leagueMembers"),where("leagueId","==",league.id)));const result=await Promise.all(snapshot.docs.map(async member=>{const data=member.data(),tips=await getTips(data.uid);let pointsTotal=0,count=0;games.forEach(game=>{pointsTotal+=points(tips[game.id],game);if(tips[game.id]&&tips[game.id].home!=null&&tips[game.id].away!=null)count++;});return{uid:data.uid,name:data.displayName||"PuckBoss",points:pointsTotal,count};}));return result.sort((a,b)=>b.points-a.points||b.count-a.count||a.name.localeCompare(b.name));}
async function renderTable(){const list=await ranking();$("tableView").innerHTML='<div class="card"><h2>Rangliste</h2><table class="table"><thead><tr><th>#</th><th>PuckBoss</th><th>Punkte</th><th>Tipps</th></tr></thead><tbody>'+list.map((entry,index)=>'<tr class="'+(entry.uid===user.uid?"me":"")+'"><td>'+(index+1)+'</td><td>'+esc(entry.name)+'</td><td><b>'+entry.points+'</b></td><td>'+entry.count+'</td></tr>').join("")+'</tbody></table></div>';}
async function renderResults(){const tips=await getTips(),done=games.filter(game=>game.homeScore!=null);if(!done.length){$("resultsView").innerHTML='<div class="card">Noch keine Ergebnisse vorhanden.</div>';return;}$("resultsView").innerHTML='<div class="card"><h2>Ergebnisse</h2>'+done.slice().reverse().map(game=>'<div class="result"><div>'+esc(String(game.date||"").slice(0,10))+'<small>'+esc(gameTime(game))+'</small></div><div class="teams"><span class="team">'+teamMarkup(game.home,true)+'</span><span class="vs">vs.</span><span class="team">'+teamMarkup(game.away,false)+'</span><br><b>'+game.homeScore+':'+game.awayScore+'</b></div><div>'+(tips[game.id]?'Tipp '+tips[game.id].home+':'+tips[game.id].away+'<br><b>'+points(tips[game.id],game)+' Punkte</b>':'Kein Tipp')+'</div></div>').join("")+'</div>';}
async function updateStats(){if(!league){$("tipCount").textContent="0";$("points").textContent="0";$("rank").textContent="–";return;}const tips=await getTips(),list=await ranking();let totalPoints=0;games.forEach(game=>totalPoints+=points(tips[game.id],game));$("tipCount").textContent=Object.keys(tips).length;$("points").textContent=totalPoints;const position=list.findIndex(entry=>entry.uid===user.uid);$("rank").textContent=position<0?"–":String(position+1);}
async function renderLeague(){await renderTips();await updateStats();}
function view(name){["tips","table","results"].forEach(x=>$(x+"View").classList.toggle("hidden",x!==name));document.querySelectorAll(".nav").forEach(button=>button.classList.toggle("active",button.dataset.view===name));if(name==="tips")renderTips();if(name==="table")renderTable();if(name==="results")renderResults();}
function authMode(value){register=value;$("loginTab").classList.toggle("active",!value);$("registerTab").classList.toggle("active",value);$("nameWrap").classList.toggle("hidden",!value);$("authSubmit").textContent=value?"Registrieren":"Anmelden";}
$("loginTab").onclick=()=>authMode(false);$("registerTab").onclick=()=>authMode(true);
$("authForm").onsubmit=async event=>{event.preventDefault();$("authMessage").textContent="";try{if(register){const credential=await createUserWithEmailAndPassword(auth,$("email").value,$("password").value);await updateProfile(credential.user,{displayName:$("displayName").value.trim()||"PuckBoss"});}else await signInWithEmailAndPassword(auth,$("email").value,$("password").value);}catch(error){console.error(error);$("authMessage").textContent=error.message.replace("Firebase: ","");}};
$("logoutBtn").onclick=async()=>{try{draftTips={};league=null;await signOut(auth);}catch(error){console.error(error);toast("Abmelden fehlgeschlagen.");}};
document.querySelectorAll(".nav").forEach(button=>button.onclick=()=>view(button.dataset.view));
onAuthStateChanged(auth,async currentUser=>{user=currentUser;draftTips={};league=null;await loadGames();if(!currentUser){$("authCard").classList.remove("hidden");$("app").classList.add("hidden");$("logoutBtn").classList.add("hidden");$("userLabel").textContent="";return;}$("authCard").classList.add("hidden");$("app").classList.remove("hidden");$("logoutBtn").classList.remove("hidden");$("userLabel").textContent=currentUser.displayName||currentUser.email;try{await ensureMainLeague();await renderLeague();}catch(error){console.error(error);$("tipsView").innerHTML='<div class="card error">Das Tippspiel konnte nicht geladen werden.</div>';}});